#include <stdio.h>

/**
 * main - this is where the outcome is to be lower case
 * Return: should return zero.
 */

int main(void)
{
	int d;

	for (d = 0; d < 10; d++)
	{
	printf("%d", d);
	}
	putchar('\n');
	return (0);
}
