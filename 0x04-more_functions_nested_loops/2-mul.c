#include "main.h"

/**
 * mul - prints capital letters
 * Return: should always return  zero or one
 * @a: checks the character int a
 * @b: checks the character int b
 */
int mul(int a, int b)
{
return (a * b);
}
