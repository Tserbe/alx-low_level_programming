#include "main.h"
/**
 * print_numbers - prints capital letters
 *
 * Return: should always return  zero or one
 */
void print_numbers(void)
{
char a;

for (a = '0'; a <= '9'; a++)
{
_putchar(a);
}
_putchar('\n');
}
