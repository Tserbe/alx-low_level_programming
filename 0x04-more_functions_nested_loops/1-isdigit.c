#include "main.h"

/**
 * _isdigit - prints capital letters
 * Return: should always return  zero or one
 * @c: checks the character
 */

int _isdigit(int c)

{
if (c >= '0' && c <= '9')
return (1);
else
return (0);
}
